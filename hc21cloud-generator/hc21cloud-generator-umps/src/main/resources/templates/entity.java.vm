package ${package.Entity};

import com.hc21cloud.common.core.support.BaseEntity;
#if(${swagger2})
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
#end
#if(${entityLombokModel})
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.Accessors;
#end
#foreach($field in ${table.fields})
    #if($field.propertyType == 'Date' && ($field.propertyName != 'createTime' && $field.propertyName != 'updateTime'))
    import java.util.Date;

    import com.alibaba.fastjson.annotation.JSONField;
        #break
    #end
#end

/**
 * <p>
 * $!{table.comment}
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
#if(${entityLombokModel})
@Data
    #if(${superEntityClass})
    @EqualsAndHashCode(callSuper = true)
    #else
    @EqualsAndHashCode(callSuper = false)
    #end
@Accessors(chain = true)
#end
#if(${swagger2})
@ApiModel(value = "${entity}对象", description = "$!{table.comment}")
#end
public class ${entity} extends BaseEntity{

private static final long serialVersionUID=1L;
## ----------  BEGIN 字段循环遍历  ----------
#foreach($field in ${table.fields})
    #if($field.propertyName != 'createBy' && $field.propertyName != 'createTime' && $field.propertyName !=
        'updateBy' && $field.propertyName != 'updateTime' && $field.propertyName != 'remark')
        #if(${field.keyFlag})
            #set($keyPropertyName=${field.propertyName})
        #end
        #if("$!field.comment" != "")
            #if(${swagger2})
            @ApiModelProperty(value = "${field.comment}")
            #else
            /**
             * ${field.comment}
             */
            #end
        #end
        #if($field.propertyType == 'Date')
        @JSONField(format = "yyyy-MM-dd HH:mm:ss")
        #end
    private ${field.propertyType} ${field.propertyName};
    #end
#end
## ----------  END 字段循环遍历  ----------

#if(!${entityLombokModel})
    #foreach($field in ${table.fields})
        #if(${field.propertyType.equals("boolean")})
            #set($getprefix="is")
        #else
            #set($getprefix="get")
        #end
        #if($field.propertyName != 'createBy' && $field.propertyName != 'createTime' && $field.propertyName !=
            'updateBy' && $field.propertyName != 'updateTime' && $field.propertyName != 'remark')
        public ${field.propertyType} ${getprefix}${field.capitalName}(){
                return ${field.propertyName};
                }
            #if(${entityBuilderModel})
            public ${entity} set${field.capitalName}(${field.propertyType} ${field.propertyName}){
            #else
            public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
            #end
                this.${field.propertyName} = ${field.propertyName};
            #if(${entityBuilderModel})
                    return this;
            #end
                }
        #end
    #end
#end

#if(!${entityLombokModel})
@Override
public String toString() {
        return "${entity}{" +
    #foreach($field in ${table.fields})
        #if($field.propertyName != 'createBy' && $field.propertyName != 'createTime' && $field.propertyName !=
            'updateBy' && $field.propertyName != 'updateTime' && $field.propertyName != 'remark')
            #if($!{foreach.index}==0)
                    "${field.propertyName}=" + ${field.propertyName} +
            #else
                    ", ${field.propertyName}=" + ${field.propertyName} +
            #end
        #end
    #end
        "}";
        }
#end
        }